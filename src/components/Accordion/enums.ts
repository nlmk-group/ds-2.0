export enum iconsMapping {
  plus = 'plus',
  arrow = 'arrow'
}

export enum sizesMapping {
  xs = 'xs',
  s = 's',
  m = 'm'
}

export enum variantsMapping {
  default = 'default',
  paper = 'paper'
}
