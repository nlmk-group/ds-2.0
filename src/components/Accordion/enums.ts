export enum EIconsAccordion {
  plus = 'plus',
  arrow = 'arrow'
}

export enum ESizesAccordion {
  xs = 'xs',
  s = 's',
  m = 'm'
}

export enum EVariantsAccordion {
  default = 'default',
  paper = 'paper'
}
