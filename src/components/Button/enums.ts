/**
 * @type {enum} ESizes
 * @type {enum} EVariant
 * @type {enum} EFill
 */

export enum ESizes {
  m = 'm',
  s = 's',
  xs = 'xs'
}

export enum EVariant {
  primary = 'primary',
  secondary = 'secondary',
  grey = 'grey',
  black = 'black'
}

export enum EFill {
  solid = 'solid',
  outline = 'outline',
  clear = 'clear'
}