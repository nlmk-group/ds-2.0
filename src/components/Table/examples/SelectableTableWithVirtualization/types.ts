export interface ISelectionWithVirtualization {
  id: number; // Идентификатор записи
  name: string; // Название сырья
  plan: number; // План (т)
  date: Date; // Срок поставки
  enterprise: string; // Предприятие
}
