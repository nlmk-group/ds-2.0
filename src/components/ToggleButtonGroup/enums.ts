/**
 * @type {enum} sizeMapping
 * @type {enum} statusMapping
 */

export enum sizeMapping {
  compact = 'compact',
  default = 'default'
}

export enum statusMapping {
  default = 'default',
  success = 'success',
  warning = 'warning',
  error = 'error'
}
