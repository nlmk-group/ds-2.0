/**
 * @type {enum} colorsMapping
 */
export enum colorsMapping {
  primary = 'primary',
  secondary = 'secondary',
  disabled = 'disabled',
  error = 'error',
  success = 'success',
  warning = 'warning',
  info = 'info',
  inherit = 'inherit',
  grey = 'grey'
}

export enum customInputColors {
  default = 'default',
  error = 'error',
  success = 'success',
  warning = 'warning'
}
