/**
 * @type {enum} colorsMapping
 */
export enum colorsMapping {
  primary = 'primary',
  secondary = 'secondary',
  disabled = 'disabled',
  error = 'error',
  success = 'success',
  warning = 'warning',
  info = 'info',
  inherit = 'inherit',
  grey = 'grey'
}

export enum customBadgeColors {
  disabled = 'disabled',
  info = 'info',
  inherit = 'inherit',
  secondary = 'secondary',
  grey = 'grey'
}

export enum customChipColors {
  disabled = 'disabled',
  info = 'info',
  inherit = 'inherit'
}

export enum customInputColors {
  default = 'default',
  error = 'error',
  success = 'success',
  warning = 'warning'
}
